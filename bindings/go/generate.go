package wcl

// To rebuild the WASM module from the Rust source, run:
//
//   go generate ./...
//
// This requires a Rust toolchain and the WCL monorepo at ../../

//go:generate sh -c "cd ../.. && just build go"
