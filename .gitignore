/target/
docs/_site/
docs/_md/
.env
# Fleeting notes captured via `wcl wdoc serve --comment` — disposable, per-machine.
.wdoc-fleeting.json
.idea/
.vscode/
.claude/worktrees/
.claude/scheduled_tasks.lock

# cargo-fuzz scratch dirs — keep the seed corpus tracked but ignore
# the artifacts produced by libfuzzer at runtime.
crates/wcl_lang/fuzz/target/
crates/wcl_lang/fuzz/artifacts/
crates/wcl_lang/fuzz/coverage/
.playwright-cli/

# Orca-managed workspace state, created by the Orca IDE per checkout.
.orca

# Local scratch: throwaway notes, prototypes and research findings.
# Never committed — anything worth keeping belongs in an issue or the book.
.scratch/

# Local bug tracking: per-checkout bug records. Never committed — anything
# worth keeping belongs in an issue.
.bug/
