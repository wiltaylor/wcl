import <wdoc.wcl>

// Has a list of all the systems registerd in this system.
// These system pages then have lots of nested pages at multiple levels:
// - Containers - these are things the next layer down in a system.
//   - Componenets - These are then the next layer down from that. These are the instances of things inside a contanier like a database, or a specific service.
//      - Code items - These are even the next layer down after that. This is diagrams of code structure, database scheamas etc.

page systems {
  
}

